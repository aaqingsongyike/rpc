package com.example.rpc.util.proxy;import org.aopalliance.aop.Advice;import org.springframework.aop.framework.ProxyFactory;public class RPCProxy {    private Class<?> clazz;    public Class<?> getClazz() {        return clazz;    }    public void setClazz(Class<?> clazz) {        this.clazz = clazz;    }    public RPCProxy() {    }    public Object getProxy(String url) {        ProxyFactory proxyFactory = new ProxyFactory();        RPCProxyFactory rpcClient = new RPCProxyFactory("HTTP", url);//        rpcClient.setDomain(url);        System.out.println(getClazz().getName());        proxyFactory.setInterfaces(getClazz());        proxyFactory.addAdvice(rpcClient);        Object proxy = proxyFactory.getProxy();        return proxy;    }}